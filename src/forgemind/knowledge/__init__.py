"""Knowledge bounded context — documents, chunks, entities, relationships.

This is the core bounded context responsible for:
- Document ingestion and parsing
- Text chunking
- Entity extraction (assets, components, failure modes, symptoms, actions)
- Relationship extraction (causes, resolved_by, has_component, symptoms_of)

Bounded Context: Knowledge
"""
